apiVersion: v1
kind: Service
metadata:
  name: { { include "geoservice-neo4j.fullname" . } }
  labels: { { - include "geoservice-neo4j.labels" . | nindent 4 } }
spec:
  type: { { .Values.service.type } }
  ports:
    - port: { { .Values.service.port } }
      targetPort: http
      protocol: TCP
      name: http
  selector: { { - include "geoservice-neo4j.selectorLabels" . | nindent 4 } }
