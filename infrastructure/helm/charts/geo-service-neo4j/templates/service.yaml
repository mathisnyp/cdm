apiVersion: v1
kind: Service
metadata:
  name: {{ include "geo-service-neo4j.fullname" . }}
  labels:
    {{- include "geo-service-neo4j.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "geo-service-neo4j.selectorLabels" . | nindent 4 }}
