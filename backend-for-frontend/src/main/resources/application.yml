server:
  servlet:
    context-path: "/bff/api"

spring:
  cloud:
    gateway:
      routes:
        - id: incident_service
          uri: http://incident-service:8080
          predicates:
            - Path=/incident/**
        - id: service2_route
          uri: http://localhost:8082  # Assuming service 2 is running on port 8082
          predicates:
            - Path=/geo/**
        - id: service3_route
          uri: http://localhost:8083  # Assuming service 3 is running on port 8083
          predicates:
            - Path=/service3/**

service_endpoints:
  auth: "${auth-service-endpoint}"

springdoc:
  api-docs:
    path: "/api-docs"
  swagger-ui:
    path: "/swagger-ui-bff.html"

management:
  endpoints:
    web:
      exposure:
        include: health,metrics,prometheus
#zuul:
#  host:
#    socket-timeout-millis: 100000
#  ribbon:
#    eager-load:
#      enabled: true
#  routes:
#    test:
#      url: http://localhost:8001
#  ssl-hostname-validation-enabled: false
