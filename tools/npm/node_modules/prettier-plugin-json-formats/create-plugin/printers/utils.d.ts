import { Expression, ObjectProperty } from '../parser';
import { ParserOptions } from 'prettier';
interface SkipOptions {
    backwards?: boolean;
}
declare const skipNewline: (text: string, index: number | false, opts?: SkipOptions) => number | false, hasNewline: (text: string, index: number, opts?: SkipOptions) => boolean, isPreviousLineEmpty: <N>(text: string, node: N, locStart: (node: N) => number) => boolean;
declare function hasTrailingComment(node: Expression): boolean;
declare function hasLeadingOwnLineComment(text: string, node: Expression | ObjectProperty, options: ParserOptions): boolean;
export { hasNewline, skipNewline, isPreviousLineEmpty, hasLeadingOwnLineComment, hasTrailingComment, };
